sudo: false
language: python
cache: pip
addons:
  apt_packages:
    - gcc-8
    - g++-8
    - swig
python:
  - "3.5"
  - "3.6"
install:
    - pip install tox tox-travis coveralls
script:
    - tox
after_success:
    - coveralls
